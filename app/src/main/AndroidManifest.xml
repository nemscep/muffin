<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nemscep.muffin">

    <application
        android:name=".MuffinApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Muffin">
        <activity android:name="com.nemscep.muffin.track.ui.expense.TrackExpenseActivity"></activity>
        <activity android:name="com.nemscep.muffin.track.ui.topup.TrackTopupActivity" />
        <activity android:name=".balances.ui.EditBalancesActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

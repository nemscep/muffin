<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/track_topup_graph"
    app:startDestination="@id/trackTopupFragment">

    <fragment
        android:id="@+id/trackTopupFragment"
        android:name="com.nemscep.muffin.track.ui.topup.TrackTopupFragment"
        android:label="fragment_track_topup"
        tools:layout="@layout/fragment_track_topup" />
    <action
        android:id="@+id/action_global_trackTopupFragment"
        app:destination="@id/trackTopupFragment"
        app:popUpTo="@id/track_topup_graph"
        app:launchSingleTop="true" />
</navigation>

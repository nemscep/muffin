<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/track_expense_graph"
    app:startDestination="@id/trackExpenseFragment">

    <fragment
        android:id="@+id/trackExpenseFragment"
        android:name="com.nemscep.muffin.track.ui.expense.TrackExpenseFragment"
        android:label="@string/track_expense"
        tools:layout="@layout/fragment_track_expense" />
    <action
        android:id="@+id/action_global_trackExpenseFragment"
        app:destination="@id/trackExpenseFragment"
        app:popUpTo="@id/track_expense_graph"
        app:launchSingleTop="true" />
</navigation>
